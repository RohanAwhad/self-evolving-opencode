# Redis cache configuration
# Used by compose.yaml and application code

# Full connection URL (preferred by most Redis clients)
REDIS_URL=redis://localhost:6380/0

# Individual components (if your client needs them separately)
REDIS_HOST=localhost
REDIS_PORT=6380
REDIS_DB=0
